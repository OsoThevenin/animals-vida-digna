---
phase: 03-forms-images-media
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validation.ts
  - src/lib/email.ts
  - src/i18n/ca.ts
  - src/i18n/es.ts
  - src/components/forms/ContactForm.tsx
  - src/pages/api/contact.ts
  - src/pages/contact.astro
  - src/pages/es/contact.astro
  - src/components/landing/ContactCtaSection.astro
  - wrangler.toml
  - .env.example
  - tests/form-validation.test.ts
  - tests/email-templates.test.ts
  - tests/contact-api.test.ts
autonomous: true
requirements:
  - FORM-01
  - FORM-03
  - FORM-04
  - FORM-05
  - FORM-06
user_setup:
  - service: resend
    why: "Email delivery for form submissions"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Verify sending domain (animalsvidadigna.org) for production"
        location: "Resend Dashboard -> Domains -> Add Domain"

must_haves:
  truths:
    - "Contact form submits data to server and receives localized success/error response"
    - "Shelter receives email notification with reply-to set to submitter"
    - "Submitter receives localized confirmation email (CA or ES)"
    - "Honeypot field silently rejects bot submissions"
    - "Rate limiting returns 429 after too many submissions from same IP"
    - "Form validation rejects missing/invalid fields on both client and server"
  artifacts:
    - path: "src/lib/validation.ts"
      provides: "Shared validation functions for contact and adoption forms"
      exports: ["validateContactForm", "validateAdoptionForm"]
    - path: "src/lib/email.ts"
      provides: "Resend email sending + localized templates"
      exports: ["sendContactNotification", "sendContactConfirmation"]
    - path: "src/components/forms/ContactForm.tsx"
      provides: "Preact island for contact form with client-side validation"
    - path: "src/pages/api/contact.ts"
      provides: "POST endpoint with honeypot, rate limiting, Resend delivery"
      exports: ["POST"]
    - path: "src/pages/contact.astro"
      provides: "Contact page (Catalan)"
    - path: "src/pages/es/contact.astro"
      provides: "Contact page (Spanish)"
  key_links:
    - from: "src/components/forms/ContactForm.tsx"
      to: "/api/contact"
      via: "fetch POST with FormData"
      pattern: "fetch.*api/contact"
    - from: "src/pages/api/contact.ts"
      to: "src/lib/email.ts"
      via: "sendContactNotification + sendContactConfirmation"
      pattern: "sendContact"
    - from: "src/pages/api/contact.ts"
      to: "src/lib/validation.ts"
      via: "validateContactForm"
      pattern: "validateContactForm"
    - from: "src/components/landing/ContactCtaSection.astro"
      to: "/contact"
      via: "href link instead of mailto"
      pattern: "href.*contact"
---

<objective>
Contact form with email delivery, shared form infrastructure, and spam protection.

Purpose: Establish the server-side form endpoint pattern for the project. This is the first API route -- contact form with Resend email delivery, honeypot anti-spam, Cloudflare rate limiting, and bilingual email templates. Also creates shared validation and email libraries that the adoption form (Plan 02) will reuse.

Output: Working contact form at /contact and /es/contact that sends emails via Resend, with localized confirmation to submitter and notification to shelter.
</objective>

<execution_context>
@/Users/pere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forms-images-media/03-CONTEXT.md
@.planning/phases/03-forms-images-media/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/i18n/index.ts:
```typescript
export type Locale = 'ca' | 'es';
export function t(locale: Locale, key: TranslationKey): string;
```

From src/i18n/ca.ts:
```typescript
// Translation keys are string literals like 'nav.contact', 'cta.contact', etc.
// Form-related keys need to be ADDED by this plan
export type TranslationKey = keyof typeof ca;
```

From src/i18n/content.ts:
```typescript
export function getLocalizedField<T>(entry: Record<string, T>, fieldName: string, locale: Locale): T;
```

From src/lib/keystatic.ts:
```typescript
export const reader = createReader(process.cwd(), keystaticConfig);
// reader.singletons.settings.read() returns settings with contactEmail field
```

From wrangler.toml (current):
```toml
name = "animals-vida-digna"
compatibility_date = "2025-08-15"
compatibility_flags = ["nodejs_compat"]

[[r2_buckets]]
binding = "IMAGES_BUCKET"
bucket_name = "animals-vida-digna-images"
```

From src/components/landing/ContactCtaSection.astro:
```astro
<!-- Currently links to mailto:info@animalsvidadigna.org — needs update to /contact page -->
```

Preact island pattern (from CatFilters.tsx):
```typescript
interface Translations { /* plain string object passed as prop */ }
interface Props {
  locale: 'ca' | 'es';
  translations: Translations;
}
// Uses useState, form handlers, fetch for API calls
```

Form field styling baseline (from CatFilters.tsx):
```
rounded-lg border border-primary/20 bg-surface px-3 py-2 text-sm text-text focus:border-primary
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared validation, email library, and i18n form strings</name>
  <files>src/lib/validation.ts, src/lib/email.ts, src/i18n/ca.ts, src/i18n/es.ts, .env.example, wrangler.toml, tests/form-validation.test.ts, tests/email-templates.test.ts</files>
  <behavior>
    - validateContactForm({ name, email, message }) returns { valid: true } for complete input
    - validateContactForm({}) returns { valid: false, errors: { name: 'required', email: 'required', message: 'required' } }
    - validateContactForm({ name: 'x', email: 'bad', message: 'hi' }) returns email format error
    - validateAdoptionForm includes same fields plus optional phone and required livingSituation
    - getContactNotificationHtml(data, 'ca') returns HTML with Catalan subject/body
    - getContactNotificationHtml(data, 'es') returns HTML with Spanish subject/body
    - getContactConfirmationHtml('ca') includes warm Catalan message "Gracies per contactar..."
    - getContactConfirmationHtml('es') includes warm Spanish message "Gracias por contactar..."
    - Email templates include reply-to field in notification data
  </behavior>
  <action>
    1. Create `src/lib/validation.ts` with:
       - `validateContactForm(data: { name?: string; email?: string; message?: string })` returns `{ valid: boolean; errors?: Record<string, string> }`
       - `validateAdoptionForm(data: { name?: string; email?: string; phone?: string; livingSituation?: string; message?: string; catName?: string })` returns same shape
       - Email validation via regex (standard pattern, not a library)
       - All error messages are keys (like 'required', 'invalid_email') not locale strings -- the API endpoint maps them to localized messages

    2. Create `src/lib/email.ts` with:
       - `sendContactNotification(resend, { name, email, message, contactEmail, locale })` -- sends to shelter with reply-to submitter
       - `sendContactConfirmation(resend, { email, name, locale })` -- sends confirmation to submitter
       - `sendAdoptionNotification(resend, { name, email, phone, livingSituation, message, catName, contactEmail, locale })` -- for Plan 02
       - `sendAdoptionConfirmation(resend, { email, name, catName, locale })` -- for Plan 02
       - HTML email templates as string functions with CA/ES switch. Warm tone per user decision.
       - From address: `Animals Vida Digna <no-reply@animalsvidadigna.org>` (use `onboarding@resend.dev` in dev)
       - Each function takes Resend instance as first arg (dependency injection for testability)

    3. Add form-related i18n keys to `src/i18n/ca.ts` and `src/i18n/es.ts`:
       - `form.name`, `form.email`, `form.message`, `form.phone`, `form.livingSituation`, `form.submit`, `form.sending`
       - `form.success.contact`, `form.success.adoption` (with {catName} placeholder for adoption)
       - `form.error.required`, `form.error.invalidEmail`, `form.error.server`, `form.error.rateLimited`
       - `form.livingSituation.flat`, `form.livingSituation.house`, `form.livingSituation.houseGarden`, `form.livingSituation.rural`, `form.livingSituation.other`
       - `contact.title`, `contact.subtitle`

    4. Add to `.env.example`: `RESEND_API_KEY=re_xxxxxxxxxxxx`

    5. Add rate limiting config to `wrangler.toml`:
       ```toml
       [[ratelimits]]
       name = "FORM_RATE_LIMITER"
       namespace_id = "1001"
         [ratelimits.simple]
         limit = 5
         period = 60
       ```

    6. Write tests first (TDD):
       - `tests/form-validation.test.ts`: Test validateContactForm and validateAdoptionForm with valid/invalid/edge cases
       - `tests/email-templates.test.ts`: Test that email template functions return HTML containing expected locale strings, correct subject lines, and reply-to data
  </action>
  <verify>
    <automated>pnpm vitest run tests/form-validation.test.ts tests/email-templates.test.ts</automated>
  </verify>
  <done>Validation functions catch all invalid inputs, email templates render in CA and ES with warm tone, i18n keys added for both locales, wrangler.toml has rate limiter config</done>
</task>

<task type="auto">
  <name>Task 2: Contact form endpoint, Preact island, and bilingual pages</name>
  <files>src/pages/api/contact.ts, src/components/forms/ContactForm.tsx, src/pages/contact.astro, src/pages/es/contact.astro, src/components/landing/ContactCtaSection.astro, tests/contact-api.test.ts</files>
  <action>
    1. Create `src/pages/api/contact.ts`:
       - `export const prerender = false;`
       - `export const POST: APIRoute` handler that:
         a. Reads FormData fields: name, email, message, locale, website (honeypot)
         b. If honeypot filled, return 200 with `{ success: true }` (silent reject)
         c. Access Cloudflare rate limiter binding. Try `context.locals.runtime.env.FORM_RATE_LIMITER` first (v12 adapter pattern per CONTEXT.md). If rate limited, return 429 with `{ success: false, error: 'rate_limited' }`
         d. Validate with `validateContactForm()` from `src/lib/validation.ts`
         e. If invalid, return 400 with `{ success: false, errors: { field: 'error_key' } }`
         f. Read shelter contactEmail from Keystatic site settings via `reader.singletons.settings.read()`
         g. Get RESEND_API_KEY from env binding (not import.meta.env)
         h. Call `sendContactNotification()` and `sendContactConfirmation()` from `src/lib/email.ts`
         i. Return 200 with `{ success: true }`
         j. Wrap in try/catch, return 500 with `{ success: false, error: 'server_error' }` on failure

    2. Create `src/components/forms/ContactForm.tsx` (Preact island):
       - Props: `{ locale: 'ca' | 'es'; translations: ContactFormTranslations }`
       - State: status ('idle' | 'submitting' | 'success' | 'error'), field errors object
       - Client-side validation using same `validateContactForm` logic (import from validation.ts)
       - On submit: prevent default, validate, show inline errors per field, if valid POST to /api/contact with FormData (include locale as hidden field)
       - Honeypot: hidden field `name="website"` with `class="absolute -left-[9999px]"` and `aria-hidden="true"` `tabindex="-1"`
       - Success state: replace form with warm success message from translations
       - Error state: show error message, keep form visible for retry
       - Submitting state: disable button, show "Sending..." text
       - Style using existing form baseline: `rounded-lg border border-primary/20 bg-surface px-3 py-2 text-sm text-text focus:border-primary`
       - Fields: name (text, required), email (email, required), message (textarea, required)

    3. Create `src/pages/contact.astro` (Catalan):
       - Import BaseLayout, ContactForm
       - Read site settings for donateUrl
       - Pass locale='ca' and translations object (extracted from i18n) to ContactForm island
       - Use `client:load` directive (form needs immediate interactivity)
       - Page layout: centered max-w-2xl, title "Contacte", subtitle from i18n, form below

    4. Create `src/pages/es/contact.astro` (Spanish):
       - Same as CA version but with locale='es'

    5. Update `src/components/landing/ContactCtaSection.astro`:
       - Change the mailto link to point to the contact page: `/${locale === 'es' ? 'es/' : ''}contact`
       - Keep same styling and CTA text

    6. Create `tests/contact-api.test.ts`:
       - Test the endpoint logic by importing and calling the handler with mocked Request objects
       - Test honeypot rejection (silent 200)
       - Test validation failure (400 with errors)
       - Test successful submission (mock Resend, verify both emails sent)
       - Note: Rate limiting test may need to mock the Cloudflare binding; if too complex to unit test, add a comment noting it's verified in integration
  </action>
  <verify>
    <automated>pnpm vitest run tests/contact-api.test.ts && pnpm astro build 2>&1 | tail -5</automated>
  </verify>
  <done>Contact form at /contact and /es/contact submits to API endpoint, honeypot silently rejects bots, validation shows inline errors, successful submission sends both notification and confirmation emails via Resend, rate limiting configured, ContactCtaSection links to /contact page, build passes</done>
</task>

</tasks>

<verification>
- `pnpm test` passes all form-related tests
- `pnpm astro build` succeeds with new API route and contact pages
- Contact form island renders with all fields and honeypot
- API endpoint handles honeypot, validation, rate limiting, and email delivery
- Email templates render correctly in both CA and ES
- ContactCtaSection links to /contact (not mailto)
</verification>

<success_criteria>
- Submitting the contact form with valid data returns success and would send emails (mocked in tests)
- Invalid submissions return localized error messages
- Honeypot-filled submissions are silently accepted but discarded
- Rate limiter config exists in wrangler.toml
- Both /contact and /es/contact pages render with the form
- All form i18n strings exist in CA and ES
- Tests cover validation, email templates, and endpoint logic
</success_criteria>

<output>
After completion, create `.planning/phases/03-forms-images-media/03-01-SUMMARY.md`
</output>
