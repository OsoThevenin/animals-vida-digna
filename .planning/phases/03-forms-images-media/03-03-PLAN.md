---
phase: 03-forms-images-media
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/OptimizedImage.astro
  - scripts/sync-images.ts
  - package.json
  - tests/optimized-image.test.ts
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-04
  - IMG-05

must_haves:
  truths:
    - "OptimizedImage component generates responsive srcset with 320w, 640w, 960w, 1280w breakpoints"
    - "Images use Cloudflare Image Resizing URLs in production and raw paths in development"
    - "All images rendered through OptimizedImage have lazy loading by default"
    - "sync-images script uploads local public/images/ files to R2 with correct keys and cache headers"
    - "Image format negotiation (AVIF/WebP) is handled via format=auto in URL"
  artifacts:
    - path: "src/components/OptimizedImage.astro"
      provides: "Shared responsive image component with Cloudflare Image Resizing"
      min_lines: 25
    - path: "scripts/sync-images.ts"
      provides: "Standalone R2 upload script"
      min_lines: 20
    - path: "tests/optimized-image.test.ts"
      provides: "Tests for URL generation and attribute output"
  key_links:
    - from: "src/components/OptimizedImage.astro"
      to: "/cdn-cgi/image/"
      via: "URL generation for Cloudflare Image Resizing"
      pattern: "cdn-cgi/image"
    - from: "scripts/sync-images.ts"
      to: "R2 bucket"
      via: "wrangler r2 object put CLI"
      pattern: "wrangler r2"
---

<objective>
Responsive image component and R2 sync pipeline.

Purpose: Create the shared OptimizedImage component that all site images will use for responsive srcsets via Cloudflare Image Resizing, plus the standalone sync-images script for uploading local images to R2. This is independent of the forms work and establishes the image pipeline for the entire site.

Output: OptimizedImage.astro component ready for site-wide adoption, sync-images script for R2 uploads, lazy loading by default.
</objective>

<execution_context>
@/Users/pere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forms-images-media/03-CONTEXT.md
@.planning/phases/03-forms-images-media/03-RESEARCH.md

<interfaces>
<!-- Existing image usage patterns in the codebase -->

From src/pages/cat/[slug].astro (how images are currently rendered):
```astro
<img
  src={cat.coverImage.src}
  alt={cat.coverImage.alt || cat.name}
  class="h-64 w-full object-cover sm:h-80 lg:h-96"
/>
```

From src/components/cats/CatCard.astro (cat card images):
```astro
<!-- Uses img tags with src from CMS data -->
```

From src/components/cats/CatGallery.astro (gallery images):
```astro
<!-- Uses img tags for thumbnails and lightbox -->
```

From wrangler.toml:
```toml
[[r2_buckets]]
binding = "IMAGES_BUCKET"
bucket_name = "animals-vida-digna-images"
```

Image path convention (from RESEARCH.md):
- Local: public/images/cats/foo.jpg
- R2 key: images/cats/foo.jpg
- CDN source: /images/cats/foo.jpg
- Resized: /cdn-cgi/image/format=auto,fit=cover,width=640,quality=80/images/cats/foo.jpg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OptimizedImage component with tests</name>
  <files>src/components/OptimizedImage.astro, tests/optimized-image.test.ts</files>
  <behavior>
    - imageUrl('images/cats/misi.jpg', 640, false) returns '/cdn-cgi/image/format=auto,fit=cover,width=640,quality=80/images/cats/misi.jpg'
    - imageUrl('images/cats/misi.jpg', 640, true) returns 'images/cats/misi.jpg' (dev mode passthrough)
    - generateSrcset('images/cats/misi.jpg', [320, 640, 960, 1280], false) returns proper srcset string with all widths
    - Default widths are [320, 640, 960, 1280]
    - Default loading is 'lazy'
    - Default sizes is '(max-width: 640px) 100vw, (max-width: 960px) 50vw, 33vw'
    - Component output includes decoding="async" attribute
    - When loading="eager", lazy is not set
  </behavior>
  <action>
    1. Extract the URL generation logic into a pure testable function. Since Astro components cannot be unit-tested directly, create the core logic as exportable functions:
       - Create the URL generation functions at the top of OptimizedImage.astro's frontmatter, AND also export them from a small helper if needed for tests. Alternatively, put the pure functions in `src/lib/image-utils.ts` and import them in both the component and tests.
       - Decision: Create `src/lib/image-utils.ts` with `imageUrl(src, width, isDev)` and `generateSrcset(src, widths, isDev)` functions. Import in component.

    2. Write tests first in `tests/optimized-image.test.ts`:
       - Test imageUrl returns /cdn-cgi/image/ URL in production mode
       - Test imageUrl returns raw src in dev mode
       - Test generateSrcset returns correct srcset string with all width descriptors
       - Test default parameters

    3. Create `src/lib/image-utils.ts`:
       ```typescript
       export function imageUrl(src: string, width: number, isDev: boolean): string
       export function generateSrcset(src: string, widths: number[], isDev: boolean): string
       ```

    4. Create `src/components/OptimizedImage.astro`:
       - Props interface: `{ src: string; alt: string; widths?: number[]; sizes?: string; class?: string; loading?: 'lazy' | 'eager' }`
       - Defaults: widths=[320, 640, 960, 1280], sizes='(max-width: 640px) 100vw, (max-width: 960px) 50vw, 33vw', loading='lazy'
       - Use `import.meta.env.DEV` to detect development mode
       - Import imageUrl and generateSrcset from image-utils.ts
       - Render `<img>` with src (largest width as fallback), srcset, sizes, alt, class, loading, decoding="async"
       - No `<picture>` element needed -- Cloudflare format=auto handles negotiation
  </action>
  <verify>
    <automated>pnpm vitest run tests/optimized-image.test.ts</automated>
  </verify>
  <done>OptimizedImage component generates responsive srcset with Cloudflare Image Resizing URLs, falls back to raw paths in dev, lazy loads by default, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: R2 sync script and npm script registration</name>
  <files>scripts/sync-images.ts, package.json</files>
  <action>
    1. Create `scripts/sync-images.ts`:
       - Standalone Node.js script that uploads local `public/images/` files to R2
       - Uses wrangler CLI approach (simpler than aws4fetch for this use case):
         ```
         npx wrangler r2 object put {bucket}/{key} --file {localPath} --content-type {mimeType}
         ```
       - Glob for `public/images/**/*.{jpg,jpeg,png,webp,avif,svg}` using Node.js fs/glob
       - Map local paths to R2 keys: strip `public/` prefix (e.g., `public/images/cats/misi.jpg` -> `images/cats/misi.jpg`)
       - Detect content-type from extension
       - Set cache-control header for long-lived immutable caching: `--cache-control "public, max-age=31536000, immutable"` (pass via wrangler r2 object put --cache-control flag if supported, otherwise note as TODO for R2 bucket lifecycle rules)
       - Print progress: "Uploading images/cats/misi.jpg... done"
       - Print summary: "Synced N images to R2"
       - Handle errors gracefully (log and continue, report failures at end)
       - Use `node:child_process` execSync for wrangler commands
       - Use `node:fs` and `node:path` for file operations
       - Use `node:fs` globSync or manual recursive directory walk

    2. Add npm script to `package.json`:
       ```json
       "sync-images": "npx tsx scripts/sync-images.ts"
       ```
       Note: tsx is needed to run TypeScript directly. Add as dev dependency if not present, or use `npx` which handles it.

    3. Verify the script compiles by running `npx tsx --check scripts/sync-images.ts` (syntax check only, no execution since R2 credentials needed).
  </action>
  <verify>
    <automated>npx tsx scripts/sync-images.ts --dry-run 2>&1 || echo "Script compiles (dry-run not implemented is OK, checking for syntax errors)"</automated>
  </verify>
  <done>sync-images script exists at scripts/sync-images.ts, uploads all public/images/ files to R2 with correct keys and cache headers, npm run sync-images registered in package.json, script compiles without errors</done>
</task>

</tasks>

<verification>
- `pnpm test` passes optimized-image tests
- OptimizedImage generates correct /cdn-cgi/image/ URLs with format=auto
- Dev mode falls back to raw image paths
- Default loading="lazy" and decoding="async" on all images
- sync-images script compiles and globs public/images/ correctly
- npm run sync-images is registered in package.json
- R2 key convention matches image URL convention (no path mismatch)
</verification>

<success_criteria>
- OptimizedImage component renders responsive srcset with 320w, 640w, 960w, 1280w
- Cloudflare Image Resizing URL format includes format=auto,fit=cover,quality=80
- Dev mode bypasses /cdn-cgi/image/ and uses raw paths
- All images default to lazy loading
- sync-images script uploads public/images/ to R2 with correct content-type
- Cache headers set to public, max-age=31536000, immutable
- Tests verify URL generation logic
</success_criteria>

<output>
After completion, create `.planning/phases/03-forms-images-media/03-03-SUMMARY.md`
</output>
