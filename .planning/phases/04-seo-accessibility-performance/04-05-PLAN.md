---
phase: 04-seo-accessibility-performance
plan: 05
type: execute
wave: 2
depends_on: [04-04]
files_modified: []
autonomous: false
gap_closure: true
requirements: [SEO-01, SEO-02, SEO-03, SEO-04, SEO-06, A11Y-01, A11Y-02, A11Y-03, A11Y-04, A11Y-05, PERF-01, PERF-02, PERF-03, PERF-04]

must_haves:
  truths:
    - "Lighthouse Performance >= 95 on mobile for CA and ES landing pages"
    - "Lighthouse Accessibility >= 95 on mobile for CA and ES landing pages"
    - "Lighthouse Best Practices >= 95 on mobile for CA and ES landing pages"
    - "Lighthouse SEO >= 95 on mobile for CA and ES landing pages (user confirmed 100)"
    - "Keyboard navigation works: skip-to-content visible on first Tab, focus rings on all interactive elements"
  artifacts: []
  key_links: []
---

<objective>
Human verification of Lighthouse scores and keyboard accessibility to confirm all PERF and A11Y requirements are met.

Purpose: The previous Lighthouse checkpoint (04-03 Task 2) was auto-approved without actual score data. This plan gets real confirmation from the user running Lighthouse in Chrome DevTools. The user has already confirmed SEO = 100, so only Performance, Accessibility, and Best Practices need confirmation.
Output: Confirmed Lighthouse scores recorded in the summary.
</objective>

<execution_context>
@/Users/pere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-accessibility-performance/04-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Lighthouse audit — Performance, Accessibility, Best Practices</name>
  <files>none (human verification only)</files>
  <read_first>
    - .planning/phases/04-seo-accessibility-performance/04-VERIFICATION.md (to understand what was already verified and what gaps remain)
  </read_first>
  <what-built>
All SEO, accessibility, and performance optimizations from Phase 04 plans 01-04:
- Complete SEO markup (canonical, OG tags, hreflang, JSON-LD, sitemap, robots.txt)
- WCAG AA contrast compliance (all text/bg pairs verified >= 4.5:1)
- Visible focus-visible styles on all interactive elements
- Skip-to-content link
- Accessible mobile menu with ARIA attributes
- System-ui font stack (no web font loading)
- Responsive images with explicit width/height via OptimizedImage
- robots.txt created (plan 04-04)

SEO score already confirmed by user: 100. Remaining categories need confirmation.
  </what-built>
  <action>
This is a human verification checkpoint. The executor should:
1. Run `pnpm build && pnpm preview` to start the preview server
2. Present the verification steps below to the user
3. Wait for user to report Lighthouse scores and keyboard test results
4. Record the scores in the plan summary

No code changes are needed. This task blocks until the user confirms scores.
  </action>
  <how-to-verify>
**Step 1: Build and preview the site**
```bash
pnpm build && pnpm preview
```
Wait for the preview server to start (default: http://localhost:4321).

**Step 2: Run Lighthouse on CA landing page**
1. Open Chrome and navigate to `http://localhost:4321/`
2. Open DevTools (F12 or Cmd+Opt+I)
3. Go to the "Lighthouse" tab
4. Settings: Mode = "Navigation", Device = "Mobile", Categories = check ALL four (Performance, Accessibility, Best Practices, SEO)
5. Click "Analyze page load"
6. Record the four scores

**Step 3: Run Lighthouse on ES landing page**
1. Navigate to `http://localhost:4321/es/`
2. Repeat the same Lighthouse audit
3. Record the four scores

**Step 4: Run Lighthouse on a cat detail page**
1. Navigate to any cat detail page, e.g. `http://localhost:4321/cat/garfield/` (use an actual cat slug from your content)
2. Repeat the same Lighthouse audit
3. Record the four scores

**Step 5: Quick keyboard navigation check**
1. On the CA landing page, press Tab once — the skip-to-content link should appear visually (fixed position bar at top)
2. Continue tabbing — all links and buttons should show a visible orange/brown outline ring
3. Resize to mobile viewport (< 768px) — Tab to the hamburger menu button and press Enter/Space — menu should open

**Expected scores:** >= 95 on Performance, Accessibility, Best Practices for all pages. SEO already confirmed = 100.

**Report your results:** Share the scores for each page in this format:
```
CA landing (/): Perf=XX, A11y=XX, BP=XX, SEO=XX
ES landing (/es/): Perf=XX, A11y=XX, BP=XX, SEO=XX
Cat detail: Perf=XX, A11y=XX, BP=XX, SEO=XX
Keyboard: skip-link=yes/no, focus-rings=yes/no, mobile-menu=yes/no
```
  </how-to-verify>
  <verify>User confirms all Lighthouse scores >= 95 on mobile for Performance, Accessibility, and Best Practices. SEO already confirmed = 100.</verify>
  <acceptance_criteria>
    - User reports Performance >= 95 on at least 2 of 3 tested pages
    - User reports Accessibility >= 95 on at least 2 of 3 tested pages
    - User reports Best Practices >= 95 on at least 2 of 3 tested pages
    - User reports SEO >= 95 on all tested pages (already confirmed = 100)
    - User confirms skip-to-content link appears on first Tab
    - User confirms focus rings visible on interactive elements
  </acceptance_criteria>
  <done>User has confirmed Lighthouse scores >= 95 on all four categories for mobile in both locales, and keyboard navigation works correctly</done>
  <resume-signal>Share your Lighthouse scores and keyboard test results. Type "approved" if all scores >= 95, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- User has confirmed actual Lighthouse scores from Chrome DevTools
- All four categories >= 95 on mobile for both locales
- Keyboard navigation confirmed working
</verification>

<success_criteria>
- Lighthouse Performance >= 95 on mobile (confirmed by user)
- Lighthouse Accessibility >= 95 on mobile (confirmed by user)
- Lighthouse Best Practices >= 95 on mobile (confirmed by user)
- Lighthouse SEO >= 95 on mobile (already confirmed = 100 by user)
- Skip-to-content and focus rings confirmed working
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-accessibility-performance/04-05-SUMMARY.md`
</output>
