---
phase: 04-seo-accessibility-performance
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/Header.astro
  - src/components/landing/StatsSection.astro
  - src/components/OptimizedImage.astro
  - src/i18n/ca.ts
  - src/i18n/es.ts
  - tests/a11y-contrast.test.ts
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04
  - A11Y-05

must_haves:
  truths:
    - "All text/background color combinations pass WCAG AA contrast (4.5:1 normal, 3:1 large)"
    - "All interactive elements show visible focus ring on keyboard Tab"
    - "Skip-to-content link appears on Tab and jumps to main content"
    - "Mobile menu button has aria-expanded, menu has aria-hidden, menu links are not focusable when hidden"
    - "All img tags have non-empty alt attributes"
    - "Gallery lightbox already has reduced-motion support via Tobii (no new work needed, just verify)"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Global :focus-visible ring style"
      contains: ":focus-visible"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Skip-to-content link and id='main-content' on <main>"
      contains: "skip"
    - path: "src/components/Header.astro"
      provides: "Accessible mobile menu with aria-expanded and aria-hidden"
      contains: "aria-expanded"
    - path: "src/components/landing/StatsSection.astro"
      provides: "Fixed contrast -- stats numbers use text-primary-dark instead of text-accent"
      contains: "text-primary-dark"
    - path: "src/components/OptimizedImage.astro"
      provides: "width and height attributes on img for CLS prevention"
      contains: "width="
    - path: "tests/a11y-contrast.test.ts"
      provides: "Programmatic contrast ratio verification for brand color pairs"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "@layer base :focus-visible rule applies globally"
      pattern: "focus-visible"
    - from: "src/components/Header.astro"
      to: "mobile-menu script"
      via: "Script toggles aria-expanded and aria-hidden attributes"
      pattern: "aria-expanded"
---

<objective>
Fix all accessibility issues: WCAG AA contrast violations, focus-visible ring styles, skip-to-content link, mobile menu keyboard accessibility, OptimizedImage width/height for CLS, and verify alt text coverage.

Purpose: Achieve WCAG AA compliance and eliminate Lighthouse accessibility deductions from contrast, focus, and keyboard navigation issues.
Output: Accessible site with passing contrast ratios, visible focus indicators, keyboard-navigable mobile menu, and CLS-preventing image dimensions.
</objective>

<execution_context>
@/Users/pere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-accessibility-performance/04-CONTEXT.md
@.planning/phases/04-seo-accessibility-performance/04-RESEARCH.md
@.planning/phases/04-seo-accessibility-performance/04-01-SUMMARY.md

<interfaces>
<!-- Contrast ratio data from RESEARCH.md -->
FAILING pairs:
- accent (#E8A87C) on surface (#FFF8F0) = 1.93:1 -- FAIL (used in StatsSection stat numbers as text-accent)
- primary (#8B5E3C) on accent (#E8A87C) = 2.74:1 -- FAIL

PASSING pairs (no changes needed):
- text (#2D1B0E) on surface (#FFF8F0) = 15.64:1
- text-muted (#6B5B4F) on surface (#FFF8F0) = 6.16:1
- primary (#8B5E3C) on surface (#FFF8F0) = 5.30:1
- primary-dark (#6B4226) on surface (#FFF8F0) = 8.20:1
- text (#2D1B0E) on accent (#E8A87C) = 8.10:1

From src/components/OptimizedImage.astro (current -- missing width/height):
```typescript
interface Props {
  src: string;
  alt: string;
  widths?: number[];
  sizes?: string;
  class?: string;
  loading?: 'lazy' | 'eager';
}
// Output: <img src= srcset= sizes= alt= class= loading= decoding="async" />
// MISSING: width and height attributes
```

From src/components/Header.astro mobile menu (current -- no ARIA):
```html
<button type="button" id="mobile-menu-btn" aria-label="Menu">...</button>
<div id="mobile-menu" class="hidden ...">...</div>
<script>
  btn.addEventListener('click', () => { menu.classList.toggle('hidden'); });
</script>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contrast ratio test and fix accent color usage</name>
  <files>tests/a11y-contrast.test.ts, src/components/landing/StatsSection.astro</files>
  <read_first>
    - src/styles/global.css (color definitions)
    - src/components/landing/StatsSection.astro (uses text-accent for stat numbers)
    - vitest.config.ts (test config)
  </read_first>
  <behavior>
    - relativeLuminance('#FFF8F0') returns approximately 0.936
    - relativeLuminance('#E8A87C') returns approximately 0.437
    - contrastRatio('#E8A87C', '#FFF8F0') returns approximately 1.93 (FAIL)
    - contrastRatio('#2D1B0E', '#FFF8F0') returns approximately 15.64 (PASS)
    - contrastRatio('#6B4226', '#FFF8F0') returns approximately 8.20 (PASS)
    - All brand color pairs used for text pass 4.5:1 minimum
    - text-accent on surface pair fails (test documents the known issue)
    - primary-dark on surface pair passes
  </behavior>
  <action>
    Create `tests/a11y-contrast.test.ts`:

    1. Implement `relativeLuminance(hex: string): number` helper inside the test file:
       - Parse hex to RGB (handle 6-digit hex)
       - Convert each channel: `c = c / 255; return c <= 0.04045 ? c / 12.92 : ((c + 0.055) / 1.055) ** 2.4`
       - `L = 0.2126 * R + 0.7152 * G + 0.0722 * B`

    2. Implement `contrastRatio(fg: string, bg: string): number` helper:
       - `const L1 = Math.max(lum1, lum2); const L2 = Math.min(lum1, lum2);`
       - `return (L1 + 0.05) / (L2 + 0.05);`

    3. Define brand colors object:
       ```typescript
       const colors = {
         text: '#2D1B0E',
         'text-muted': '#6B5B4F',
         primary: '#8B5E3C',
         'primary-dark': '#6B4226',
         accent: '#E8A87C',
         surface: '#FFF8F0',
       };
       ```

    4. Test all actively-used text/background pairs pass 4.5:1:
       - text on surface (15.64:1 -- PASS)
       - text-muted on surface (6.16:1 -- PASS)
       - primary on surface (5.30:1 -- PASS)
       - primary-dark on surface (8.20:1 -- PASS)
       - text on accent (8.10:1 -- PASS, accent used as button bg)

    5. Document the accent-on-surface failure with a test that asserts it FAILS (confirming the fix is needed):
       - `expect(contrastRatio(colors.accent, colors.surface)).toBeLessThan(3)` -- proves accent must not be used as text on surface

    Then fix `src/components/landing/StatsSection.astro`:
    - Line 28: Change `text-accent` to `text-primary-dark` on the stats number `<p>` element
    - Before: `<p class="font-display text-4xl font-bold text-accent sm:text-5xl">`
    - After: `<p class="font-display text-4xl font-bold text-primary-dark sm:text-5xl">`

    Scan for other `text-accent` usages in Astro/TSX components that render on light backgrounds and fix them similarly. Known safe usages: accent as background (button bg) where text color is `text` (#2D1B0E).
  </action>
  <verify>
    <automated>cd /Users/pere/code/animals-vida-digna && pnpm vitest run tests/a11y-contrast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/a11y-contrast.test.ts exists with relativeLuminance and contrastRatio helpers
    - Test file verifies all 5 actively-used text/bg pairs pass >= 4.5 ratio
    - Test file includes assertion that accent on surface fails (< 3.0)
    - StatsSection.astro line with stat numbers contains `text-primary-dark` not `text-accent`
    - All tests pass (exit code 0)
    - No remaining `text-accent` class used on elements with surface background (grep verification)
  </acceptance_criteria>
  <done>All text/background color combinations pass WCAG AA contrast. Stats numbers use primary-dark instead of accent.</done>
</task>

<task type="auto">
  <name>Task 2: Add focus-visible styles, skip-to-content link, accessible mobile menu, and OptimizedImage dimensions</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro, src/components/Header.astro, src/components/OptimizedImage.astro, src/i18n/ca.ts, src/i18n/es.ts</files>
  <read_first>
    - src/styles/global.css (current content -- add focus-visible layer)
    - src/layouts/BaseLayout.astro (add skip-to-content before Header, add id="main-content" to main)
    - src/components/Header.astro (mobile menu button and div -- add ARIA attributes)
    - src/components/OptimizedImage.astro (add width/height props and attributes)
    - src/i18n/ca.ts (add a11y.skipToContent translation key)
    - src/i18n/es.ts (add a11y.skipToContent translation key)
  </read_first>
  <action>
    **Step 1: Global focus-visible styles**

    Add to `src/styles/global.css` after the `@theme` block:
    ```css
    @layer base {
      :focus-visible {
        outline: 2px solid var(--color-primary);
        outline-offset: 2px;
      }
    }
    ```

    **Step 2: Skip-to-content link**

    Add translation keys to `src/i18n/ca.ts`:
    ```typescript
    'a11y.skipToContent': 'Saltar al contingut',
    ```

    Add to `src/i18n/es.ts`:
    ```typescript
    'a11y.skipToContent': 'Saltar al contenido',
    ```

    In `src/layouts/BaseLayout.astro`, add as the FIRST child of `<body>`, before `<Header>`:
    ```html
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:rounded-lg focus:bg-primary focus:px-4 focus:py-2 focus:text-surface focus:outline-none"
    >
      {t(locale, 'a11y.skipToContent')}
    </a>
    ```

    Change `<main class="flex-1">` to `<main id="main-content" class="flex-1">`.

    **Step 3: Accessible mobile menu in Header.astro**

    Update the mobile menu button to include `aria-expanded="false"`:
    ```html
    <button
      type="button"
      class="inline-flex items-center justify-center rounded-lg p-2 text-text-muted transition-colors hover:text-primary md:hidden"
      aria-label="Menu"
      aria-expanded="false"
      aria-controls="mobile-menu"
      id="mobile-menu-btn"
    >
    ```

    Update the mobile menu div to include `aria-hidden="true"` and `hidden` attribute:
    ```html
    <div id="mobile-menu" class="border-t border-primary/10 md:hidden" hidden aria-hidden="true">
    ```
    Remove the `hidden` CSS class from the div (use HTML `hidden` attribute instead for true accessibility).

    Update the script to toggle both ARIA and hidden:
    ```html
    <script>
      const btn = document.getElementById('mobile-menu-btn');
      const menu = document.getElementById('mobile-menu');
      if (btn && menu) {
        btn.addEventListener('click', () => {
          const isExpanded = btn.getAttribute('aria-expanded') === 'true';
          btn.setAttribute('aria-expanded', String(!isExpanded));
          menu.setAttribute('aria-hidden', String(isExpanded));
          if (isExpanded) {
            menu.setAttribute('hidden', '');
          } else {
            menu.removeAttribute('hidden');
          }
        });
      }
    </script>
    ```

    **Step 4: OptimizedImage width/height for CLS prevention**

    Update `src/components/OptimizedImage.astro` Props interface to accept optional width and height:
    ```typescript
    interface Props {
      src: string;
      alt: string;
      widths?: number[];
      sizes?: string;
      class?: string;
      loading?: 'lazy' | 'eager';
      width?: number;
      height?: number;
    }
    ```

    Destructure the new props with defaults:
    ```typescript
    const {
      src,
      alt,
      widths = DEFAULT_WIDTHS,
      sizes = DEFAULT_SIZES,
      class: className,
      loading = 'lazy',
      width = widths[widths.length - 1],  // default to largest width
      height,
    } = Astro.props;
    ```

    Update the `<img>` tag to include width and conditionally height:
    ```html
    <img
      src={fallbackSrc}
      srcset={srcset}
      sizes={sizes}
      alt={alt}
      class={className}
      loading={loading}
      decoding="async"
      width={width}
      {...(height ? { height } : {})}
    />
    ```

    The `width` attribute always renders (defaults to largest srcset width: 1280). The `height` is optional -- callers that know aspect ratio can pass it. For CLS, having `width` plus CSS `aspect-ratio` or `height` via CSS class (e.g., `h-64`) is sufficient in modern browsers.

    **Step 5: Verify alt text coverage (A11Y-04)**

    The CMS schema enforces alt text fields, and OptimizedImage requires `alt` prop. The `<img>` for the logo in Header.astro already has `alt="Animals Vida Digna"`. No changes needed -- just verify grep shows no `<img` without `alt` in .astro and .tsx files.
  </action>
  <verify>
    <automated>cd /Users/pere/code/animals-vida-digna && pnpm vitest run && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/global.css contains `:focus-visible` rule with `outline: 2px solid var(--color-primary)`
    - src/layouts/BaseLayout.astro contains `href="#main-content"` skip link as first child of body
    - src/layouts/BaseLayout.astro `<main>` tag has `id="main-content"`
    - src/components/Header.astro mobile button has `aria-expanded="false"` and `aria-controls="mobile-menu"`
    - src/components/Header.astro mobile menu div has `hidden` attribute and `aria-hidden="true"`
    - src/components/Header.astro script toggles `aria-expanded` and `aria-hidden` on click
    - src/components/OptimizedImage.astro Props interface includes `width?: number` and `height?: number`
    - src/components/OptimizedImage.astro `<img>` tag includes `width={width}` attribute
    - src/i18n/ca.ts contains `'a11y.skipToContent': 'Saltar al contingut'`
    - src/i18n/es.ts contains `'a11y.skipToContent': 'Saltar al contenido'`
    - `pnpm build` completes without errors
    - No `<img` tag without `alt` attribute in src/**/*.astro or src/**/*.tsx files
  </acceptance_criteria>
  <done>Focus-visible styles globally applied, skip-to-content link works in both locales, mobile menu is keyboard accessible with ARIA, OptimizedImage outputs width attribute for CLS prevention, all images have alt text.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run` -- all tests pass (including a11y-contrast)
- `pnpm build` -- no build errors
- Grep: no `text-accent` on surface backgrounds in component files
- Grep: all `<img` tags have `alt` attributes
</verification>

<success_criteria>
- All text/background pairs pass WCAG AA 4.5:1 contrast ratio
- Every interactive element shows visible focus ring on keyboard Tab
- Skip-to-content link present in both CA and ES locales
- Mobile menu properly toggles aria-expanded/aria-hidden
- OptimizedImage outputs width attribute for CLS prevention
- All images have non-empty alt attributes
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-accessibility-performance/04-02-SUMMARY.md`
</output>
