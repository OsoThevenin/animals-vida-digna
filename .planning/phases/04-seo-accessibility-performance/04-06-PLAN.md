---
phase: 04-seo-accessibility-performance
plan: 06
type: execute
wave: 3
depends_on: [04-05]
files_modified: []
autonomous: false
gap_closure: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04]

must_haves:
  truths:
    - "Lighthouse Performance >= 95 on mobile for CA landing page (confirmed by user)"
    - "Lighthouse Accessibility >= 95 on mobile for CA landing page (confirmed by user)"
    - "Lighthouse Best Practices >= 95 on mobile for CA landing page (confirmed by user)"
    - "Lighthouse SEO >= 95 on mobile for CA landing page (user already confirmed 100)"
  artifacts: []
  key_links: []
---

<objective>
Human verification gate: confirm Lighthouse mobile scores >= 95 on all four categories after performance fixes.

Purpose: Performance was measured at 65 before fixes. Plan 04-05 applied structural optimizations (LCP image, CLS fixes, lazy Tobii, deferred hydration). This checkpoint confirms the fixes achieved the target >= 95 score.
Output: User-confirmed Lighthouse scores recorded in the summary.
</objective>

<execution_context>
@/Users/pere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-accessibility-performance/04-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Lighthouse mobile audit — all four categories >= 95</name>
  <files>none (human verification only)</files>
  <read_first>
    - .planning/phases/04-seo-accessibility-performance/04-VERIFICATION.md
  </read_first>
  <what-built>
Performance optimizations applied in plan 04-05:
- Hero image now uses OptimizedImage with fetchpriority="high", explicit width/height, and srcset
- About and Colonies section images now use OptimizedImage with explicit dimensions (prevents CLS)
- Tobii lightbox JS is now dynamically imported instead of eagerly loaded
- CatFilters images now have width/height attributes (prevents CLS)
- DonateSticky hydration deferred from client:only to client:idle (reduces TBT)
- robots.txt created in plan 04-04

Previous scores (before fixes):
- Performance: 65 (FAILING)
- Accessibility: >= 95 (PASSING)
- Best Practices: >= 95 (PASSING)
- SEO: 100 (PASSING)
  </what-built>
  <action>
This is a human verification checkpoint. The executor should:
1. Run `pnpm build && pnpm preview` to start the preview server
2. Present the verification steps below to the user
3. Wait for user to report Lighthouse scores
4. If Performance < 95, ask user to share the specific failing metrics (LCP, TBT, CLS values) so further fixes can be planned
5. Record the scores in the plan summary
  </action>
  <how-to-verify>
**Step 1: Build and preview**
```bash
pnpm build && pnpm preview
```
Wait for preview server (default: http://localhost:4321).

**Step 2: Lighthouse on CA landing page**
1. Open Chrome, navigate to `http://localhost:4321/`
2. Open DevTools (F12 or Cmd+Opt+I)
3. Go to "Lighthouse" tab
4. Settings: Mode = "Navigation", Device = "Mobile", Categories = ALL four
5. Click "Analyze page load"
6. Record the four scores

**Step 3: Lighthouse on ES landing page**
1. Navigate to `http://localhost:4321/es/`
2. Repeat same Lighthouse audit
3. Record the four scores

**Expected:** Performance >= 95, Accessibility >= 95, Best Practices >= 95, SEO >= 95 on both pages.

**If Performance < 95:** Please share the Lighthouse "Diagnostics" section — specifically the LCP time, Total Blocking Time, and Cumulative Layout Shift values. This will help plan further fixes if needed.

**Report format:**
```
CA landing (/): Perf=XX, A11y=XX, BP=XX, SEO=XX
ES landing (/es/): Perf=XX, A11y=XX, BP=XX, SEO=XX
```
  </how-to-verify>
  <verify>User confirms all Lighthouse scores >= 95 on mobile for both CA and ES landing pages.</verify>
  <acceptance_criteria>
    - User reports Performance >= 95 on CA landing page
    - User reports Performance >= 95 on ES landing page
    - User reports Accessibility >= 95 on both pages
    - User reports Best Practices >= 95 on both pages
    - User reports SEO >= 95 on both pages (already confirmed 100)
  </acceptance_criteria>
  <done>User has confirmed Lighthouse scores >= 95 on all four categories for mobile on both CA and ES landing pages.</done>
  <resume-signal>Share your Lighthouse scores for both pages. Type "approved" if all >= 95, or share the failing metrics so we can plan additional fixes.</resume-signal>
</task>

</tasks>

<verification>
- User has run Lighthouse on both CA and ES landing pages
- All four categories score >= 95 on mobile
- Scores recorded in plan summary
</verification>

<success_criteria>
- Lighthouse Performance >= 95 on mobile (confirmed by user) for CA and ES
- Lighthouse Accessibility >= 95 on mobile (confirmed by user)
- Lighthouse Best Practices >= 95 on mobile (confirmed by user)
- Lighthouse SEO >= 95 on mobile (already confirmed = 100)
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-accessibility-performance/04-06-SUMMARY.md`
</output>
