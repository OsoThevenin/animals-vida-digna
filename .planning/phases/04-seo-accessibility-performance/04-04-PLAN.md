---
phase: 04-seo-accessibility-performance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [public/robots.txt]
autonomous: true
gap_closure: true
requirements: [SEO-05]

must_haves:
  truths:
    - "robots.txt is served at /robots.txt with correct User-agent, Allow, and Sitemap directives"
  artifacts:
    - path: "public/robots.txt"
      provides: "Static robots.txt for crawlers"
      contains: "Sitemap: https://animalsvidadigna.org/sitemap-index.xml"
  key_links:
    - from: "public/robots.txt"
      to: "sitemap-index.xml"
      via: "Sitemap directive URL"
      pattern: "Sitemap:.*sitemap-index\\.xml"
---

<objective>
Create the missing robots.txt file to close the SEO-05 gap.

Purpose: The Cloudflare SSR adapter does not auto-generate robots.txt. A static file in public/ is the simplest fix and ensures crawlers can discover the sitemap.
Output: public/robots.txt served at the site root.
</objective>

<execution_context>
@/Users/pere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-accessibility-performance/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public/robots.txt</name>
  <files>public/robots.txt</files>
  <read_first>
    - public/ (directory listing to confirm no existing robots.txt)
    - astro.config.mjs (to confirm site URL used in Sitemap directive)
  </read_first>
  <action>
Create the file `public/robots.txt` with exactly these three lines (no trailing newline after the last line, one newline between each line):

```
User-agent: *
Allow: /
Sitemap: https://animalsvidadigna.org/sitemap-index.xml
```

The Sitemap URL uses `sitemap-index.xml` because `@astrojs/sitemap` generates a sitemap index file at that path. The domain is `animalsvidadigna.org` matching the `site` value in `astro.config.mjs`.

After creating the file, run `pnpm build` to verify it appears in the build output. With the Cloudflare adapter, the file should be copied to the output as a static asset.
  </action>
  <verify>
    <automated>test -f public/robots.txt && grep -q "User-agent: \*" public/robots.txt && grep -q "Allow: /" public/robots.txt && grep -q "Sitemap: https://animalsvidadigna.org/sitemap-index.xml" public/robots.txt && echo "PASS: robots.txt exists with correct content"</automated>
  </verify>
  <acceptance_criteria>
    - public/robots.txt exists
    - Line 1 is exactly: User-agent: *
    - Line 2 is exactly: Allow: /
    - Line 3 is exactly: Sitemap: https://animalsvidadigna.org/sitemap-index.xml
    - File has exactly 3 lines
    - `pnpm build` completes without errors
  </acceptance_criteria>
  <done>robots.txt exists in public/ with User-agent, Allow, and Sitemap directives pointing to the correct sitemap-index.xml URL</done>
</task>

</tasks>

<verification>
- `cat public/robots.txt` shows the three expected lines
- `pnpm build` succeeds
- robots.txt is included in the build output
</verification>

<success_criteria>
- public/robots.txt exists with correct content
- SEO-05 requirement is satisfied
- Build completes successfully with robots.txt included
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-accessibility-performance/04-04-SUMMARY.md`
</output>
