---
phase: 01-foundation-cms-i18n
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - keystatic.config.tsx
  - src/lib/keystatic.ts
  - src/content/cats/.gitkeep
  - src/content/pages/.gitkeep
  - src/content/landing/.gitkeep
  - src/content/settings/.gitkeep
  - src/pages/keystatic/[...params].astro
  - tests/schemas.test.ts
autonomous: true
requirements:
  - FOUND-03
  - CMS-01
  - CMS-02
  - CMS-03
  - CMS-04
  - CMS-05

must_haves:
  truths:
    - "Keystatic admin loads at /keystatic during dev and allows creating/editing content"
    - "Settings singleton has all localized fields (siteName, SEO, social links, donateUrl)"
    - "Cats collection preserves all fields from existing Payload schema with _ca/_es localization"
    - "Landing singleton supports reorderable typed section blocks"
    - "Pages collection supports localized MDX content for legal/privacy pages"
    - "Every image field has alt_ca and alt_es text fields"
  artifacts:
    - path: "keystatic.config.tsx"
      provides: "Full CMS schema definition for settings, cats, landing, pages"
      contains: "config("
    - path: "src/lib/keystatic.ts"
      provides: "Keystatic reader instance for content access"
      contains: "createReader"
    - path: "src/pages/keystatic/[...params].astro"
      provides: "Keystatic admin route (SSR, dev-only)"
      contains: "prerender = false"
  key_links:
    - from: "src/lib/keystatic.ts"
      to: "keystatic.config.tsx"
      via: "import keystaticConfig"
      pattern: "import.*keystatic\\.config"
    - from: "src/pages/keystatic/[...params].astro"
      to: "@keystatic/astro/internal"
      via: "makeHandler import"
      pattern: "makeHandler"
---

<objective>
Define all Keystatic CMS content schemas (settings, cats, landing sections, static pages) with bilingual field patterns, create the reader API helper, and set up the admin route.

Purpose: Establish the complete content model so shelter staff can manage all site content through Keystatic, and downstream plans can read content via typed helpers.
Output: Working Keystatic admin with all schemas, content directories, and reader API.
</objective>

<execution_context>
@/Users/pere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-cms-i18n/01-CONTEXT.md
@.planning/phases/01-foundation-cms-i18n/01-RESEARCH.md
@.planning/phases/01-foundation-cms-i18n/01-01-SUMMARY.md

@src/cms/collections/Cats/index.ts (existing Payload cat schema -- reference for field porting)

<interfaces>
<!-- From Plan 01 outputs that this plan depends on -->
From astro.config.mjs:
- Conditional Keystatic/React/Markdoc loading in dev
- Cloudflare adapter configured
- Preact integration (no compat mode)

From keystatic reader API (to be created):
```typescript
// src/lib/keystatic.ts
import { createReader } from '@keystatic/reader';
import keystaticConfig from '../../keystatic.config';
export const reader = createReader(process.cwd(), keystaticConfig);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Keystatic config with all content schemas and admin route</name>
  <files>keystatic.config.tsx, src/pages/keystatic/[...params].astro, src/content/cats/.gitkeep, src/content/pages/.gitkeep, src/content/landing/.gitkeep, src/content/settings/.gitkeep</files>
  <action>
Create `keystatic.config.tsx` at project root with the full content model. Use `storage: { kind: 'local' }`. All admin labels in Catalan.

**Settings singleton** (`src/content/settings/global`):
- siteName_ca, siteName_es (text, required)
- logo (image, directory: 'public/images')
- primaryColor (text)
- donateUrl (url, label: 'URL de donacio (Teaming)')
- contactEmail (text)
- social object: facebook, instagram, twitter (all url fields)
- seo object: title_ca, title_es, description_ca (multiline), description_es (multiline), image (image field with alt_ca, alt_es)

**Cats collection** (`src/content/cats/*`, slugField: 'slug_ca'):
Port ALL fields from existing Payload schema (src/cms/collections/Cats/index.ts). Add _ca/_es suffixes for localized fields:
- slug_ca (slug field), slug_es (text)
- name_ca, name_es (text, required)
- race_ca, race_es (text, optional -- breed name may differ)
- status (select: available/adopted/treatment/unavailable, label: 'Estat')
- age (integer, label: 'Edat (anys)')
- gender (select: male/female, label: 'Genere')
- size (select: small/medium/large, label: 'Mida')
- personality (multiselect: playful/calm/shy/affectionate/independent/social/curious/protective, label: 'Personalitat')
- goodWith (multiselect: children/other-cats/dogs/elderly, label: 'Es porta be amb')
- healthStatus (select: healthy/treatment/special-needs, label: 'Estat de Salut')
- vaccinated, microchipped, sterilized (checkbox fields)
- weight (number, optional)
- rescueDate (date, label: 'Data de Rescat')
- adoptionDate (date, optional, label: "Data d'Adopcio")
- specialNeeds_ca, specialNeeds_es (text, multiline, optional)
- observations_ca, observations_es (text, multiline, optional)
- coverImage object: src (image, directory: 'public/images/cats'), alt_ca (text), alt_es (text)
- gallery (array of objects: src image + alt_ca + alt_es)
- shortDescription_ca, shortDescription_es (text, multiline)
- description_ca, description_es (markdoc document fields for MDX content)
- featured (checkbox, label: 'Destacat')
- order (integer, default: 0, label: 'Ordre')
- seo object: title_ca, title_es, description_ca, description_es

**Landing singleton** (`src/content/landing/home`):
Use `fields.blocks` for the sections array with these block types:
- hero: title_ca/es, subtitle_ca/es, image (with alt_ca/es), ctaAdoptText_ca/es, ctaDonateText_ca/es
- about: title_ca/es, content_ca/es (markdoc), image (with alt_ca/es)
- stats: title_ca/es, items array (label_ca/es, value text)
- colonies: title_ca/es, content_ca/es (markdoc), image (with alt_ca/es)
- adopt: title_ca/es, subtitle_ca/es
- collaborate: title_ca/es, content_ca/es (markdoc), ctaText_ca/es
- contactCta: title_ca/es, subtitle_ca/es, ctaText_ca/es
- newsletter: title_ca/es, subtitle_ca/es
- faq: title_ca/es (text), items (array of objects with question_ca/es + answer_ca/es text fields)

If `fields.blocks` is not available or has issues, fall back to `fields.array` of `fields.conditional` with a discriminant field for section type. Document which approach was used.

Note: The FAQ block type is included in the schema per CONTEXT.md decisions (landing section block types). The FAQ UI rendering/accordion is deferred to v2, but the content model includes it now so content can be prepared.

**Pages collection** (`src/content/pages/*`, slugField: 'slug'):
- slug (slug field)
- title_ca, title_es (text, required)
- content_ca, content_es (markdoc document fields)
- seo object: title_ca, title_es, description_ca, description_es

**Admin route** at `src/pages/keystatic/[...params].astro`:
- `export const prerender = false;`
- Import makeHandler from '@keystatic/astro/internal'
- Import keystaticConfig from root
- Create handler and return response

Create `.gitkeep` files in content directories: `src/content/cats/`, `src/content/pages/`, `src/content/landing/`, `src/content/settings/`.
  </action>
  <verify>
    <automated>cd /Users/pere/code/animals-vida-digna && pnpm build</automated>
  </verify>
  <done>
    - keystatic.config.tsx defines settings singleton, cats collection, landing singleton, pages collection
    - All localized fields use _ca/_es suffix pattern
    - Every image field has alt_ca and alt_es
    - Landing singleton includes all 9 block types: hero, about, stats, colonies, adopt, collaborate, contactCta, newsletter, faq
    - Cats collection preserves ALL fields from existing Payload schema (personality multiselect, goodWith, health booleans, featured, order, gallery, etc.)
    - Admin route exists at src/pages/keystatic/[...params].astro with prerender = false
    - `pnpm build` still succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Keystatic reader helper and Wave 0 schema tests</name>
  <files>src/lib/keystatic.ts, tests/schemas.test.ts</files>
  <action>
1. Create `src/lib/keystatic.ts`:
   ```typescript
   import { createReader } from '@keystatic/reader';
   import keystaticConfig from '../../keystatic.config';
   export const reader = createReader(process.cwd(), keystaticConfig);
   ```

2. Create `tests/schemas.test.ts` covering CMS-01 through CMS-05:

Import the keystatic config directly and validate schema structure:
```typescript
import keystaticConfig from '../keystatic.config';
```

Test cases:
- **CMS-01**: settings singleton exists, has siteName_ca, siteName_es, donateUrl, contactEmail, social object, seo object with localized fields
- **CMS-02**: cats collection exists, has slug_ca (slugField), all localized fields (name_ca/es, description_ca/es, shortDescription_ca/es, specialNeeds_ca/es, observations_ca/es, race_ca/es), all shared fields (status, age, gender, size, personality, goodWith, healthStatus, vaccinated, microchipped, sterilized, weight, rescueDate, adoptionDate, featured, order), coverImage, gallery
- **CMS-03**: landing singleton exists, has sections field, sections support hero/about/stats/colonies/adopt/collaborate/contactCta/newsletter/faq block types (all 9 types must be present)
- **CMS-04**: pages collection exists, has slug, title_ca/es, content_ca/es, seo object
- **CMS-05**: coverImage in cats has alt_ca and alt_es fields. Settings seo image has alt fields. Verify pattern across all image-bearing schemas.

Test approach: Access `keystaticConfig.singletons.settings.schema` and `keystaticConfig.collections.cats.schema` etc. to verify field existence. This tests schema structure without needing a running Keystatic instance.
  </action>
  <verify>
    <automated>cd /Users/pere/code/animals-vida-digna && pnpm vitest run tests/schemas.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - src/lib/keystatic.ts exports reader instance
    - tests/schemas.test.ts passes all CMS-01..05 tests
    - CMS-03 test asserts all 9 block types including faq
    - Schema tests validate field existence for all content types
    - `pnpm vitest run` exits 0
  </done>
</task>

</tasks>

<verification>
- `pnpm build` exits 0 with Keystatic config loaded
- `pnpm vitest run tests/schemas.test.ts` exits 0
- keystatic.config.tsx has all 4 content types (settings, cats, landing, pages)
- Landing singleton has all 9 block types including faq
- All image fields have bilingual alt text
</verification>

<success_criteria>
Complete CMS content model defined in Keystatic with all schemas matching the existing Payload data model, reader API helper ready for content access, and schema validation tests passing. Keystatic admin accessible at /keystatic in dev mode.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cms-i18n/01-02-SUMMARY.md`
</output>
