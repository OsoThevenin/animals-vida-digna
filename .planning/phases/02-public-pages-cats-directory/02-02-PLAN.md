---
phase: 02-public-pages-cats-directory
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/cats/CatFilters.tsx
  - src/components/cats/CatGallery.astro
  - src/components/cats/CatTraits.astro
  - src/pages/cats/index.astro
  - src/pages/cat/[slug].astro
  - src/pages/es/cats/index.astro
  - src/pages/es/cat/[slug].astro
  - tests/cats-filter.test.ts
  - tests/cats-routes.test.ts
autonomous: true
requirements:
  - CATS-01
  - CATS-02
  - CATS-03
  - CATS-04

must_haves:
  truths:
    - "Cats listing page displays all cats with working client-side filters for status, age, sex, and temperament"
    - "Base HTML of cats listing is crawlable by search engines (all cat cards in server-rendered HTML)"
    - "Cat detail pages show cover image, gallery with lightbox, traits, and full markdoc description in the correct locale"
    - "Gallery lightbox supports keyboard navigation (Tab, Escape, Arrow keys)"
    - "Cat detail pages work at /cat/[slug_ca] and /es/cat/[slug_es] with correct locale content"
  artifacts:
    - path: "src/components/cats/CatFilters.tsx"
      provides: "Preact island for client-side cat filtering"
      exports: ["default"]
    - path: "src/pages/cats/index.astro"
      provides: "Catalan cats listing with SSR base"
      contains: "reader.collections.cats"
    - path: "src/pages/cat/[slug].astro"
      provides: "Catalan cat detail with dynamic route"
      contains: "getStaticPaths"
    - path: "src/pages/es/cats/index.astro"
      provides: "Spanish cats listing"
    - path: "src/pages/es/cat/[slug].astro"
      provides: "Spanish cat detail with dynamic route"
    - path: "src/components/cats/CatGallery.astro"
      provides: "Gallery component with Tobii lightbox"
      contains: "tobii"
    - path: "src/components/cats/CatTraits.astro"
      provides: "Cat traits display (status, age, gender, health, etc.)"
  key_links:
    - from: "src/components/cats/CatFilters.tsx"
      to: "cat data"
      via: "JSON prop from Astro page"
      pattern: "cats.*filter"
    - from: "src/pages/cat/[slug].astro"
      to: "reader.collections.cats"
      via: "getStaticPaths + read with resolveLinkedFiles"
      pattern: "reader\\.collections\\.cats"
    - from: "src/components/cats/CatGallery.astro"
      to: "Tobii lightbox"
      via: "client script initialization"
      pattern: "new Tobii"
---

<objective>
Build the cats directory: a listing page with SSR-rendered cat cards enhanced by a Preact filter island for client-side filtering, and detail pages with cover image, lightbox gallery, traits display, and markdoc description rendering per locale.

Purpose: Lets visitors browse all cats, filter by attributes, and view detailed profiles -- the core adoption discovery experience.
Output: Working cats listing at /cats and /es/cats with filters, cat detail pages at /cat/[slug] and /es/cat/[slug] with gallery and full content.
</objective>

<execution_context>
@/Users/pere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-pages-cats-directory/02-RESEARCH.md
@.planning/phases/02-public-pages-cats-directory/02-01-SUMMARY.md

@keystatic.config.tsx
@src/lib/keystatic.ts
@src/lib/markdoc.ts
@src/i18n/index.ts
@src/i18n/content.ts
@src/i18n/ca.ts
@src/components/cats/CatCard.astro
@src/layouts/BaseLayout.astro

<interfaces>
<!-- From plan 02-01: CatCard and markdoc helper created there -->

From src/lib/markdoc.ts (created in 02-01):
```typescript
export async function renderMarkdoc(asyncContent: () => Promise<{ node: unknown }>): Promise<string>;
```

From src/i18n/content.ts:
```typescript
export function getLocalizedCat(catEntry: Record<string, unknown>, locale: Locale): {
  name: string; slug: string; race: string; shortDescription: string;
  description: unknown; specialNeeds: string; observations: string;
  status: string; age: number | null; gender: string; size: string;
  personality: string[]; goodWith: string[]; healthStatus: string;
  vaccinated: boolean; microchipped: boolean; sterilized: boolean;
  weight: number | null; rescueDate: string | null; adoptionDate: string | null;
  featured: boolean; order: number;
  coverImage: { src: string; alt: string } | null;
  gallery: Array<{ src: string; alt: string }>;
  seo: { title: string; description: string } | null;
};
```

From keystatic.config.tsx cats collection:
- slug_ca: slug field (collection key)
- slug_es: text field (manual)
- status: select ('available' | 'adopted' | 'treatment' | 'unavailable')
- gender: select ('male' | 'female')
- personality: multiselect ('playful' | 'calm' | 'shy' | 'affectionate' | 'independent' | 'social' | 'curious' | 'protective')
- description_ca/es: markdoc fields (async)
- gallery: array of bilingualImage objects

From src/i18n/ca.ts (translation keys for cat fields):
- cat.status, cat.status.available, cat.status.adopted, cat.status.treatment, cat.status.unavailable
- cat.gender, cat.male, cat.female
- cat.age, cat.size, cat.personality, cat.weight, etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cats listing page with SSR base and Preact filter island</name>
  <files>src/components/cats/CatFilters.tsx, src/pages/cats/index.astro, src/pages/es/cats/index.astro, tests/cats-filter.test.ts</files>
  <behavior>
    - CATS-01: CatFilters with statusFilter='available' returns only cats with status 'available'
    - CATS-01: CatFilters with genderFilter='female' returns only female cats
    - CATS-01: CatFilters with multiple filters applies AND logic
    - CATS-01: CatFilters with all filters set to 'all' returns all cats
    - CATS-05: Filtering by featured=true returns only featured cats (reuse filter logic test)
  </behavior>
  <action>
1. Create `tests/cats-filter.test.ts` with unit tests for the filter logic. Extract the filtering function into a pure testable function (either in the CatFilters.tsx file as a named export, or in a separate `src/lib/cat-filters.ts` utility). Test cases:
   - Filter by status: 'available', 'adopted', 'all'
   - Filter by gender: 'male', 'female', 'all'
   - Filter by personality trait (e.g., 'playful')
   - Combined filters (status=available AND gender=female)
   - No matching results returns empty array
   - 'all' value on all filters returns all cats

2. Create `src/components/cats/CatFilters.tsx` -- Preact island:
   - Props: `{ cats: CatFilterData[], locale: 'ca' | 'es' }`
   - CatFilterData interface: `{ keystatic_slug, name, slug, status, age, gender, personality, coverImage, shortDescription }`
   - State: statusFilter, genderFilter, personalityFilter (all default to 'all')
   - Filter controls rendered as `<select>` dropdowns with localized option labels
   - Use the translation keys from ca.ts/es.ts for filter labels (pass translated strings as data attributes or additional props since this is a Preact island and can't use the t() function directly -- instead, pass a `translations` object prop with needed strings)
   - Filtered cats rendered as a grid of cards (3 cols lg, 2 cols sm, 1 col mobile)
   - Each card: image, name, short description, status badge, links to detail page
   - The card markup in the Preact island should closely match the CatCard.astro component styling for visual consistency
   - Show result count ("N gats" / "N gatos")
   - Show "no results" message when filters match nothing

3. Create `src/pages/cats/index.astro` (Catalan listing, CATS-02):
   - Read all cats via `reader.collections.cats.all()`
   - Map through getLocalizedCat with locale 'ca', add keystatic_slug
   - Sort by order field
   - Render page title, brief intro
   - Pass serializable cat data (only filter-relevant fields, NOT markdoc content) as JSON prop to CatFilters island with `client:load`
   - IMPORTANT for SSR/SEO (CATS-02): Also render a `<noscript>` fallback or hidden server-rendered list of all cat cards using CatCard.astro so that search engine crawlers see all cats in the raw HTML. Approach: render CatCard grid in a div with `class="hidden"` and add `aria-hidden="true"`, while the Preact island renders the interactive version. Alternatively, render all cats server-side and let the Preact island take over rendering (the Preact island fully owns display when JS is available).
   - Wrap in BaseLayout, pass donateUrl from settings

4. Create `src/pages/es/cats/index.astro` (Spanish listing):
   - Same as above but with locale 'es'

Run tests to verify filter logic works.
  </action>
  <verify>
    <automated>pnpm vitest run tests/cats-filter.test.ts && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Cats listing pages at /cats and /es/cats render all cats server-side for SEO, Preact filter island provides client-side filtering by status/gender/personality, filter logic unit tests pass, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create cat detail pages with markdoc rendering, gallery lightbox, and traits display</name>
  <files>src/components/cats/CatGallery.astro, src/components/cats/CatTraits.astro, src/pages/cat/[slug].astro, src/pages/es/cat/[slug].astro, tests/cats-routes.test.ts</files>
  <action>
1. Create `tests/cats-routes.test.ts` with tests for CATS-03 slug mapping:
   - Test that getStaticPaths generates paths using slug_ca for Catalan routes
   - Test that getStaticPaths generates paths using slug_es for Spanish routes
   - Test that a cat with slug_ca="michi" and slug_es="michi-es" generates correct paths
   - Extract the path-generation logic into a testable pure function

2. Create `src/components/cats/CatTraits.astro`:
   - Props: `{ cat: LocalizedCat, locale: Locale }` where LocalizedCat is the return type of getLocalizedCat
   - Display traits in a structured layout (grid or definition list):
     - Status (with colored badge: green=available, grey=adopted, yellow=treatment)
     - Age (formatted with locale string: "3 anys" / "3 anos")
     - Gender (translated via t())
     - Size (translated)
     - Race
     - Weight (if present, formatted with "kg")
     - Personality tags (as pill badges)
     - Good with (as pill badges)
     - Health status (translated)
     - Medical info: vaccinated, microchipped, sterilized (checkmarks)
     - Rescue date (if present)
   - Use Tailwind for clean, scannable layout

3. Create `src/components/cats/CatGallery.astro` (CATS-04):
   - Props: `{ images: Array<{ src: string, alt: string }>, catName: string }`
   - Render a grid of thumbnail images (4 cols on desktop, 2 on mobile)
   - Each thumbnail wrapped in `<a href="{full-image-url}" class="lightbox" data-group="cat-gallery">`
   - Inside each anchor: `<img src="{src}" alt="{alt}" loading="lazy" class="rounded-lg object-cover" />`
   - Include a `<script>` tag (not a Preact island) that:
     - Dynamically imports Tobii: `import Tobii from '@midzer/tobii'`
     - Imports Tobii CSS: `import '@midzer/tobii/dist/tobii.min.css'`
     - Initializes: `new Tobii({ captions: true, zoom: false })`
   - NOTE: Using Astro `<script>` tag (which is bundled and runs client-side) is simpler than a Preact island for Tobii since Tobii operates on DOM elements directly. No cleanup needed since Astro pages are full navigations.
   - Reduced-motion: Tobii respects prefers-reduced-motion natively. Add a CSS rule to disable any custom transitions under `@media (prefers-reduced-motion: reduce)`.

4. Create `src/pages/cat/[slug].astro` (Catalan detail, CATS-03):
   - `getStaticPaths()`: read all cats, return paths where `params.slug` = `entry.slug_ca || slug` (the keystatic slug IS slug_ca) and `props.keystatic_slug = slug`
   - In page: read the specific cat via `reader.collections.cats.read(keystatic_slug, { resolveLinkedFiles: true })`
   - Get localized cat data via getLocalizedCat(entry, 'ca')
   - Render markdoc description: use renderMarkdoc on the description field. With resolveLinkedFiles, check if it's already resolved (has `.node` property) or still an async function. Handle both cases.
   - Layout: cover image (full width or hero-style), then two-column layout (traits sidebar + description main content), then gallery below
   - Include "Adopt me" CTA button linking to the adoption form (Phase 3 -- for now link to contact section or mailto)
   - Include "Back to all cats" link to /cats
   - SEO: use cat's seo.title and seo.description in BaseLayout title/description props
   - Pass donateUrl from settings to BaseLayout

5. Create `src/pages/es/cat/[slug].astro` (Spanish detail):
   - Same structure but locale='es'
   - `getStaticPaths()` uses `entry.slug_es` for the slug param (fall back to keystatic slug if slug_es is empty)
   - IMPORTANT: handle case where slug_es is empty/undefined -- fall back to the keystatic slug

6. Add new i18n keys to `src/i18n/ca.ts` and `src/i18n/es.ts` if needed:
   - 'cat.backToList': 'Tornar a tots els gats' / 'Volver a todos los gatos'
   - 'cat.adoptMe': "Adopta'm" / 'Adoptame'
   - 'cat.gallery': 'Galeria' / 'Galeria'
   - 'cat.years': 'anys' / 'anos'
   - 'cat.kg': 'kg' / 'kg'
   - 'cats.title': 'Gats' / 'Gatos'
   - 'cats.showing': 'Mostrant' / 'Mostrando'
   - 'cats.noResults': 'No s\'han trobat gats' / 'No se encontraron gatos'
   - 'cats.filterStatus': 'Estat' / 'Estado'
   - 'cats.filterGender': 'Genere' / 'Genero'
   - 'cats.filterPersonality': 'Personalitat' / 'Personalidad'
   - 'cats.filterAll': 'Tots' / 'Todos'

Run `pnpm test` to verify all tests pass, then `pnpm build` to verify build succeeds.
  </action>
  <verify>
    <automated>pnpm test && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Cat detail pages render at /cat/[slug] and /es/cat/[slug] with cover image, traits sidebar, markdoc description, and gallery with Tobii lightbox. Slug routing uses locale-specific slugs. All tests pass, build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- `pnpm test` passes all tests (filter logic, route generation, markdoc, landing, donate)
- Cats listing at /cats shows all cats with filter controls
- Cats listing HTML source contains all cat entries (SEO crawlable)
- Cat detail page renders cover image, traits, description, and gallery
- Gallery thumbnails open in Tobii lightbox with keyboard navigation
- /cat/[slug_ca] shows Catalan content, /es/cat/[slug_es] shows Spanish content
</verification>

<success_criteria>
- CatFilters Preact island provides working client-side filtering by status, gender, and personality
- Server-rendered cat cards in listing page HTML for SEO
- Cat detail pages use locale-specific slugs from CMS
- Markdoc descriptions render as formatted HTML
- Gallery uses Tobii lightbox with accessible keyboard navigation
- All i18n keys added for cat-related UI strings
- Build succeeds, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-pages-cats-directory/02-02-SUMMARY.md`
</output>
